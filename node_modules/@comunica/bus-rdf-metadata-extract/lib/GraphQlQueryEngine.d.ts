import type { IQueryEngine } from '@comunica/types';
import type { IQueryEngine as IGraphQlQueryEngine } from 'graphql-ld';
import type { Algebra } from 'sparqlalgebrajs';
/**
 * A comunica-based GraphQL-LD query engine.
 */
export declare class GraphQlQueryEngine implements IGraphQlQueryEngine {
    private readonly comunicaEngine;
    constructor(comunicaEngine: IQueryEngine);
    query(query: Algebra.Operation, options?: any): Promise<any>;
}
