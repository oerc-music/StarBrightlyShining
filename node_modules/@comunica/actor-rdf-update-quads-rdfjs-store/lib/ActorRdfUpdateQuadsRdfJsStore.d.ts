import type { IActionRdfUpdateQuads, IActorRdfUpdateQuadsOutput, IQuadDestination } from '@comunica/bus-rdf-update-quads';
import { ActorRdfUpdateQuadsDestination } from '@comunica/bus-rdf-update-quads';
import type { ActionContext, IActorArgs, IActorTest } from '@comunica/core';
/**
 * A comunica RDFJS Store RDF Update Quads Actor.
 */
export declare class ActorRdfUpdateQuadsRdfJsStore extends ActorRdfUpdateQuadsDestination {
    constructor(args: IActorArgs<IActionRdfUpdateQuads, IActorTest, IActorRdfUpdateQuadsOutput>);
    test(action: IActionRdfUpdateQuads): Promise<IActorTest>;
    protected getDestination(context: ActionContext): Promise<IQuadDestination>;
}
